import styled from 'styled-components';

const Header = styled.div`
  display: flex;
  justify-content: space-between;

  // This is for the animation effect
  overflow: hidden;
`;

export default Header;
